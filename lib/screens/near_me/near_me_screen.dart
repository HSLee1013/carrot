import 'package:flutter/material.dart';

class NearMeScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    print("Near Me Screen build");
    return Scaffold(
      body: Center(child: Text("Near Me Screen")),
    );
  }
}
